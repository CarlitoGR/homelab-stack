---
edge_caddy_version: "2.11"
edge_authelia_version: "4.39"
edge_dir: "{{ nas_docker_root }}/edge"
# Host port suffix for URLs: empty on 443, ":8443" otherwise.
edge_port_suffix: "{{ '' if edge_https_port | int == 443 else ':' ~ edge_https_port }}"
edge_fetch_root_cert: true
