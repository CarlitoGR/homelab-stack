---
all:
  children:
    nas:
      hosts:
        ds1525:
          ansible_host: 192.168.1.10 # CHANGE: the NAS's LAN (or Tailscale) IP
          ansible_user: nasadmin # CHANGE: a DSM account in the administrators group
          ansible_port: 22
